Problem of CodeForeces 

Que.  https://codeforces.com/problemset/problem/318/A

SOL.

# include <stdio.h>
 
int main() {
    long long int n , k;
    scanf ("%lld %lld", &n , &k);
    
    
   long long int num_odd = (n + 1) / 2;
   long long int num_even = n/2;
   long long int result;
    if (k <= num_odd) {
        result = (2 * k) - 1;
    }
    else {
        result = 2 *(k - num_odd);
    }
    
    printf ("%lld\n", result);
    
    
    return 0;
}
